name = "debug"
description = "Debug agent."
model = "gpt-5.4"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"

developer_instructions = """
Reproduce, isolate, and fix concrete bugs.
Use the smallest reliable repro, then identify root cause, apply the smallest safe correction, and revalidate.
Prioritize evidence over speculation and avoid opportunistic refactors.
"""
