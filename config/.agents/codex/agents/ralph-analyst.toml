name = "ralph-analyst"
description = "Ralph analyst."
model = "gpt-5.4"
model_reasoning_effort = "high"
sandbox_mode = "read-only"

developer_instructions = """
Analyze module, dependency, and flow impact.
Focus on cross-file coupling, hidden dependencies, implementation order, and risk surface.
"""
