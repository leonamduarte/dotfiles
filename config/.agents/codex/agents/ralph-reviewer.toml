name = "ralph-reviewer"
description = "Ralph reviewer."
model = "gpt-5.4"
model_reasoning_effort = "high"
sandbox_mode = "read-only"

developer_instructions = """
Review the diff or scope for regressions, correctness issues, and merge-readiness concerns.
Findings come first and should be ordered by severity.
Include exact file and line references when possible.
If no issues are found, say so explicitly and note any residual risk.
"""
