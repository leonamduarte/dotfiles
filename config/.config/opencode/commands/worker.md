---
description: Run the low-cost copilot-worker for simple or medium local changes.
agent: copilot-worker
subtask: true
---

Execute the current request as a simple or medium-scope implementation, debug, or safe refactor task.

Expected behavior:
- choose the smallest relevant implementation, debug, or simplification path
- keep changes local and low risk
- escalate to `implementer` if the task becomes complex
- run only narrow validation and report what changed
