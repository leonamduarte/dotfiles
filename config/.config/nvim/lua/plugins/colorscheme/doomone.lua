return {
  {
    "bashln/Doom-One.nvim",
    lazy = false,
    priority = 1000,
    opts = {
      transparent = true,
      colors = {},
      highlights = {},
    },
    -- config = function(_, opts)
    --   require("doom-one").setup(opts)
    --   vim.cmd.colorscheme("doom-one")
    -- end,
  },
}
