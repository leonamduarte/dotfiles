# ============================================================
# DEFAULT SESSION CONFIGURATION
# ============================================================
# Session padrão do kitty - similar ao TMUX default session
# ============================================================

# Start with a single window in the home directory
launch

# You can add more windows/tabs with specific commands:
# launch --type=tab --title "Logs" tail -f /var/log/syslog
# launch --type=tab --title "Editor" nvim
# launch --location=hsplit htop
# launch --location=vsplit bash
