return {
  {
    "bashln/nyoom.nvim",
    enabled = true,
    lazy = false,
    priority = 1000,
    dir = "/run/media/dev/stow/bashln/bashln/gitlab/nyoom.nvim",
    config = function()
      require("nyoom").setup({
        colorscheme = "oxocarbon",
        apply_default_options = false,
        load_core_autocmds = false,
        load_example_config = false,
        load_local_modules = true,
      })
    end,
  },
}
